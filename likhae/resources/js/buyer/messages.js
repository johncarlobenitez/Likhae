import{$,$$,escapeHtml}from"./utils.js";
export function initBuyerMessages(){const p=$("[data-buyer-messages]");if(!p)return;$$("[data-conversation]",p).forEach(c=>c.addEventListener("click",()=>{$$("[data-conversation]",p).forEach(x=>x.classList.remove("is-active"));c.classList.add("is-active");$("[data-chat-name]",p).textContent=c.dataset.name||"Seller";p.classList.remove("show-list")}));const f=$("[data-chat-form]",p),i=$("[data-chat-input]",p),b=$("[data-chat-body]",p);f?.addEventListener("submit",e=>{e.preventDefault();const t=i.value.trim();if(!t)return;b.insertAdjacentHTML("beforeend",`<div class="b-bubble-row mine"><div class="b-bubble">${escapeHtml(t)}<time>Just now</time></div></div>`);i.value="";b.scrollTop=b.scrollHeight});$("[data-show-conversations]",p)?.addEventListener("click",()=>p.classList.add("show-list"))}
